Докер-образ- шаблон будущего докера-контейнера(как класс для объекта).
Докер-контейнер-запущенный контейнер по шаблону докер-образа(как объект для класса)


Шаги:
0) сформировать мануальный алгоритм запусков тестов и репорта(через profiles в помнике)
1) Создать Докер-образ на основе тестов(шаблон)
2) Запускать автотесты на основе докер-образа внутри докер-контейнера
3) Достать репорт тестов после прогона из контейнера
4) Сложить результат в специальную папку результатов


Зачем?     Кейс использования
AQA1 -> разрабатывают тесты (вся техника: java, mvn и тд)
AQA2 -> разрабатывают тесты (вся техника: java, mvn и тд)
DEV1 -> просто запускать тесты (без понимания техники)
DEV1 -> просто запускать тесты (без понимания техники)

AQA1-> упаковывают новую версию в докер ("коробку")
AQA2-> упаковывают новую версию в докер ("коробку")


DEV1 -> берут и запускают обновленные тесты из "коробки" (используя докер)
DEV2 -> берут и запускают обновленные тесты из "коробки" (используя докер)

